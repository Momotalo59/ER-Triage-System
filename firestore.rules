
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // เปิดสิทธิ์ให้อ่านและเขียนข้อมูลได้ทั้งหมดในช่วงการพัฒนาเพื่อความรวดเร็ว
      allow read, write: if true;
    }
  }
}
