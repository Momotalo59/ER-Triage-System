rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // อนุญาตให้อ่านและเขียนข้อมูลได้ทั้งหมดเพื่อความสะดวกในช่วงการพัฒนาต้นแบบ (Prototype)
      allow read, write: if true;
    }
  }
}